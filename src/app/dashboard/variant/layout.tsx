import { Metadata } from "next";
import Page from "./page";

export const metadata: Metadata = {
  title: "Image Variant || Keystone Engineering Consultant",
  description: "This is the image variant page",
};
export default Page;
