import { Metadata } from "next";
import Page from "./page";

export const metadata: Metadata = {
  title: "Shape Builder || Keystone Engineering Consultant",
  description: "This is the Shape Builder",
};
export default Page;
